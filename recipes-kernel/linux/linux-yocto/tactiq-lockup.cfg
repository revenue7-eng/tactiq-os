# TactiQ OS kernel availability fragment: turn silent hangs into panics.
# Applied to every machine via linux-yocto_%.bbappend, next to tactiq-security.cfg.
#
# Why (measured on rock5a): the hardware watchdog is sound. With
# /dev/watchdog0 opened and never fed, dw_wdt resets the board after ~89 s
# (2^31 cycles at 24 MHz, the TOP=15 ceiling). But the kernel shipped with no
# lockup detector at all, so a stuck CPU, a task hung in D-state, or an oops
# in a driver never becomes a panic. PID 1 keeps feeding the watchdog from a
# live CPU and the board sits half-dead with no reset. These options close
# the gap between "something hung" and "panic reboots into the next A/B
# attempt".
#
# The 89 s watchdog ceiling is what sizes the hung-task timeout below: a
# reset by an unfed watchdog leaves no panic text, so detection has to win
# the race against it, not merely happen.
#
# Regression: echo c > /proc/sysrq-trigger must reboot within panic timeout.
# kill -STOP 1 is NOT a test: the kernel ignores default-action signals sent
# to the global init from userspace, so systemd never stops feeding.

# Softlockup: a CPU that does not schedule for 20+ s (BOOTPARAM sets the
# default of kernel.softlockup_panic=1).
CONFIG_LOCKUP_DETECTOR=y
CONFIG_SOFTLOCKUP_DETECTOR=y
CONFIG_BOOTPARAM_SOFTLOCKUP_PANIC=y

# Hardlockup: a CPU with interrupts stuck off. Kconfig picks the perf (PMU)
# or buddy implementation for the target; verify after build via
# /proc/sys/kernel/nmi_watchdog and dmesg.
CONFIG_HARDLOCKUP_DETECTOR=y

# Hung task: uninterruptible task (D-state on a dead eMMC/SPI path). 30 s,
# not the 120 s default: khungtaskd wakes on the timeout interval, so
# detection can lag by up to two timeouts. At 120 s a hang that also stops
# PID 1 from feeding the watchdog is taken by the 89 s hardware reset first,
# and a watchdog reset carries no panic text.
CONFIG_DETECT_HUNG_TASK=y
CONFIG_DEFAULT_HUNG_TASK_TIMEOUT=30
CONFIG_BOOTPARAM_HUNG_TASK_PANIC=y

# Oops in kernel code is a panic, not a killed task with a corrupted kernel.
# The cmdline duplicate (panic_on_oops=1) is not set here: it belongs to
# bootargs, not to kernel config. This platform has two independent bootargs
# producers, extlinux.conf and /chosen written by tactiq-slot-dtb.bb.
# Measured on rock5a via /proc/cmdline: extlinux wins. The line read
# root=PARTLABEL=rootfs_a with no /dev/dm-0 and no panic_on_oops, and panic=5
# was already present. The image measured was not built from this tree
# (kernel g018906fdca00, ima_appraise=log), so the producer identity is
# established, the values of that particular image are not this tree's.
# If the duplicate is wanted, it goes in UBOOT_EXTLINUX_KERNEL_ARGS.
CONFIG_PANIC_ON_OOPS=y
