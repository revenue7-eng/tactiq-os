# TactiQ OS — U-Boot env on raw microSD
# Window: u-boot.itb ends ~9.2 MiB, p1 (boot_a) starts at 12 MiB
# MUST match CONFIG_ENV_OFFSET / CONFIG_ENV_SIZE in u-boot env-mmc.cfg
/dev/mmcblk1	0xB00000	0x8000
