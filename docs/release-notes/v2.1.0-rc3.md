# TactiQ OS v2.1.0-rc3 — Release Notes

**Status:** Release candidate. Canonical successor to `v2.1.0-rc2`.
**Tag:** `v2.1.0-rc3` at commit `5adb346`.
**Machine targets shipped:** `tactiq-rock5a` (RK3588S).
**Yocto release:** `scarthgap`. Kernel: `linux-yocto` 6.6 LTS.

---

## What this release is

`v2.1.0-rc3` is the first TactiQ OS release whose artifacts are signed
under a **workflow identity** rather than a personal account — the
`release-sign.yml` workflow that ran on GitHub Actions at tag-push
time, using Sigstore keyless signing. The trust root for the rc3
signature is the publicly reviewable workflow source preserved at
`.github/workflows/release-sign.yml` in this repository combined with
the Sigstore Rekor transparency log entry (index `1361817475`); it is
not a personal email account.

The binary payload itself is unchanged from `v2.1.0-rc2`: no Yocto
recipe, machine configuration, kernel fragment, or distro flag was
touched between the two tags. See
[`docs/reproducibility/v2.1.0-rc3.md`](../reproducibility/v2.1.0-rc3.md)
for the identity statement. `v2.1.0-rc3` is therefore a re-signing and
documentation release, and it supersedes `v2.1.0-rc2` for all
consumption going forward.

## Verification

The canonical verification procedure for this release is in
[`VERIFY.md`](../../VERIFY.md). It covers fetching the release assets
from Codeberg, verifying SHA-256 integrity against the signed
`SHA256SUMS`, verifying the Sigstore workflow-identity signature
(`SHA256SUMS.workflow.pem` + `SHA256SUMS.workflow.sig`) against the
expected certificate identity, and inspecting the SBOM and CVE
artifacts.

Rekor transparency-log index for this release's cosign workflow-identity
signature: **`1361817475`**. Web UI:
<https://search.sigstore.dev/?logIndex=1361817475>.

The full status of the SLSA build-provenance attestation file with
respect to consumer-side reproducibility on the Codeberg-hosted release
path is in [`VERIFY.md`](../../VERIFY.md) §5.

## Changes since v2.1.0-rc2

### Supply chain
- Release-signing workflow
  ([`.github/workflows/release-sign.yml`](../../.github/workflows/release-sign.yml),
  preserved as a record of the run that signed `rc3`) was the source of
  the `SHA256SUMS` signature for this release. It ran on tag push,
  signed with Sigstore keyless under workflow identity, verified the
  signature against the expected SAN (`refs/tags/v2.1.0-rc3`), and
  uploaded the signature assets to the release.
- The `workflow_dispatch` trigger was **removed** from
  `release-sign.yml` before `rc3`. Manual dispatch runs from the
  default branch and produces a certificate with a `refs/heads/main`
  SAN — a cryptographically valid signature that does not bind to the
  tag, and thus is not a valid release signature. This class of
  breakage occurred on `v2.1.0-rc2` and motivated the rc3 re-signing
  release (see the deprecation notice below).

### Documentation
- [`VERIFY.md`](../../VERIFY.md) — full verification procedure, two
  identity paths (personal for rc2 and earlier, workflow for rc3
  onward), expected SAN strings, failure examples, Rekor lookup.
- [`docs/reproducibility/v2.1.0-rc3.md`](../reproducibility/v2.1.0-rc3.md)
  — binary-identity statement pointing at the unchanged rc2 methodology
  and raw data.
- [`docs/release-notes/v2.1.0-rc3.md`](v2.1.0-rc3.md) — this file.
- [`docs/release-notes/v2.1.0-rc2-deprecated.md`](v2.1.0-rc2-deprecated.md)
  — rc2 deprecation rationale.
- `SECURITY.md` supported-versions table updated to reflect rc2
  deprecation and rc3 canonical status.

### Binary artifacts
- Root filesystem, kernel, device tree, manifest, buildinfo, SBOM
  bundle, aggregate SPDX, and CVE reports are **bit-for-bit identical**
  to the corresponding rc2 artifacts.
  `rootfs-rock5a.ext4` SHA-256:
  `55425ccc8b6d2a01ee9824fe96436ebb632b5a25ecc504d0ff4f7cca71b7e0e4`.

## Release artifacts

| File                                   | Description                                   |
|----------------------------------------|-----------------------------------------------|
| `rootfs-rock5a.ext4`                   | Hardened root filesystem for Rock 5A          |
| `kernel-rock5a.bin`                    | linux-yocto 6.6 LTS kernel image              |
| `rk3588s-rock-5a.dtb`                  | Device tree blob                              |
| `manifest-rock5a.txt`                  | Plain-text installed package list             |
| `buildinfo-rock5a.json`                | Build metadata (date, machine, layer hash)    |
| `sbom-image-rock5a.spdx.tar.zst`       | SPDX 2.2, per-recipe (896 documents)          |
| `sbom-image-rock5a-aggregate.spdx.json`| SPDX aggregate: 763 packages, 7,178 files     |
| `cve-manifest-rock5a.json`             | Yocto-format CVE manifest                     |
| `cve-full-rock5a.json.gz`              | Full CVE report, JSON                         |
| `cve-full-rock5a.txt.gz`               | Full CVE report, plain text                   |
| `cve-image-rock5a.txt`                 | Per-image CVE rollup                          |
| `SHA256SUMS`                           | SHA-256 of every artifact above               |
| `SHA256SUMS.workflow.pem`              | Fulcio cert, workflow identity (rc3+)         |
| `SHA256SUMS.workflow.sig`              | cosign signature over `SHA256SUMS`            |

## Hardware support and performance reporting

Target platforms: Rock 5A (RK3588S), Rock 5B (RK3588), Rock 5T (RK3588),
generic ARM64 via Yocto machine configs. Additional RK3588-family
boards and a `qemu-x86_64` configuration are supported for build and
functional validation.

Performance metrics are published according to the TactiQ measurement
standard: multi-sample variance across board units, thermal envelopes
under sustained load, and runtime data from representative workloads.
Publication follows the full qualification cycle. Board-specific
qualification data, once released, will be linked from future release
notes.

## Deprecation of v2.1.0-rc2

`v2.1.0-rc2` is **deprecated in favor of `v2.1.0-rc3`**. The root cause
is a Rekor transparency-log entry created under the `refs/heads/main`
SAN by a manual `workflow_dispatch` run of the release-signing
workflow, which is not a valid release identity and cannot be removed
from the log. The affected workflow-identity signature assets were
deleted from the rc2 release, but the Rekor entry persists; the
cleanest remediation is to publish a tag-triggered canonical signature
on a new tag. Full rationale, including the rc2 personal-identity
signature that remains valid for anyone who prefers to continue using
rc2, is in
[`docs/release-notes/v2.1.0-rc2-deprecated.md`](v2.1.0-rc2-deprecated.md).

## Known limitations — roadmap to GA

This is a release candidate, not GA. The following items are
acknowledged gaps; each is tracked and has a named milestone rather
than a date. GA will be cut when every item below is either closed or
has an explicit, documented rationale for staying out of scope.

- **Full threat model document.** In progress. The individual
  components — SELinux policy, RAUC signing, IMA posture, attestation
  agent, release-signing pipeline — are each documented in place;
  a consolidated threat model tying them into a single adversary
  model is not yet published.
- **SLSA binary attestation.** The historical attestation
  (`actions/attest-build-provenance@v2` in `attest.yml`, run on GitHub
  Actions for `rc3`) binds to the deterministic source archive only.
  Binary-level attestation — for `rootfs-rock5a.ext4` and friends —
  requires the full Yocto build to run inside a hosted CI runtime.
  Roadmap: port the lint and attestation pipeline to a Codeberg-native
  CI (Forgejo Actions or Woodpecker), then stand up a minimal rootfs
  build (qemu-x86_64) there, extending to Rock 5A afterwards. Tracked
  in [`SUPPLY_CHAIN.md`](../../SUPPLY_CHAIN.md#build-provenance).
- **Hermetic builder / SLSA L3 path.** The hosted runner used by the
  historical `attest.yml` (stock `ubuntu-24.04` on GitHub Actions) is
  sufficient for L2 but not L3. Reaching L3 requires a hermetic builder
  with controlled network and tooling. Not in scope for GA; explicitly
  roadmapped.
- **Dependency pinning audit.** Action versions in the historical
  GitHub Actions workflows are pinned by Dependabot configuration in
  `.github/dependabot.yml`; the equivalent pinning discipline will be
  carried over when the pipeline is ported to Codeberg-native CI. The
  Yocto layer pins via `scarthgap` release and a specific kernel
  point-release. A full audit confirming that every transitive tool
  version used during the build is hash-pinned, with the same digests
  across independent builds, has not been published. Tracked.
- **SELinux policy supply chain.** Custom SELinux policy modules
  (eight `(.te, .if, .fc)` triplets under `tactiq_*`) live in the
  sibling repository
  [`revenue7-eng/tactiq-os-selinux`](https://codeberg.org/revenue7-eng/tactiq-os-selinux).
  That repository has CI syntax checks but does not yet carry the same
  audit-ready release regime as this one: no tagged cosign-signed
  releases, no `VERIFY.md`, no Rekor-anchored signature over compiled
  policy artefacts. The `rootfs-rock5a.ext4` shipped here contains the
  compiled policy, and its constituent recipes are listed in the SPDX
  aggregate, but a consumer cannot today independently verify a chain
  from this rootfs back to a signed policy source tag. Bringing the
  policy repo under the same signing, `VERIFY.md`, and reproducibility
  discipline is the first scheduled supply-chain item beyond rc3, and
  is a precondition for the combined trust root claimed at GA.
- **Key rotation and revocation policy.** Sigstore keyless removes
  long-lived signing keys entirely, which eliminates most rotation
  surface. The remaining signing keys are the RAUC development
  keyring (in-tree, explicitly dev-only) and any future production
  keyring loaded from CI secrets. A written rotation and revocation
  policy for the production RAUC keyring is pending and will ship
  with the production-keyring work tracked in `SUPPLY_CHAIN.md`.
- **Hardware qualification matrix.** Per the TactiQ measurement
  standard (see README and the Hardware section above), qualification
  data is released as part of the qualification cycle, not ahead of
  it. The rc3 release ships functional artifacts; qualified
  performance figures are tracked separately.
- **Filesystem-image bit-identity.** 99.9433% byte-identity
  empirically measured in rc2 and unchanged in rc3. Remaining drift
  is localised to known non-deterministic ext4 metadata regions;
  driving this to 100% requires `mkfs.ext4 --uuid` pinning, inode
  timestamp normalisation, and deterministic `machine-id` /
  `random-seed` generation. Upstream Yocto work is in flight; we
  plan to adopt as it lands.
- **Two-person review gate on builder config.** Not wired today.
  Roadmap item tracked in `SUPPLY_CHAIN.md`.

Every item above is surfaced so consumers can make an informed trust
decision about this release candidate. Items are added to, not removed
from, the "Known limitations" section as they are identified; the
section only shrinks when a gap is actually closed.

## Contact

Security-sensitive reports: see [`SECURITY.md`](../../SECURITY.md).
